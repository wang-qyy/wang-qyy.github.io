import styles from './index.less';

const MyTemplate = () => {
  return (
    <div className={styles.wrap}>
      <div className={styles.title}>最近保存的项目</div>
    </div>
  );
};

export default MyTemplate;
